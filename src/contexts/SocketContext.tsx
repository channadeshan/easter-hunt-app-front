/**
 * SocketContext.tsx — DEPRECATED
 * ─────────────────────────────────────────────────────────────────────────────
 * The socket is now a global singleton in src/socket.ts.
 * Import it directly wherever you need it:
 *
 *   import socket from '../socket';
 *
 * This file is kept as a no-op so any lingering import doesn't break the build.
 * It can be safely deleted once you've confirmed nothing imports from it.
 * ─────────────────────────────────────────────────────────────────────────────
 */

export {}; // empty module — nothing to export
